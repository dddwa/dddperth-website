import styled from '@emotion/styled'
import { zIndex } from 'components/utils/styles/zindex'

export const StyledVotingPanel = styled.div`
  position: sticky;
  top: 0;
  z-index: ${zIndex.votingPanel};
`
