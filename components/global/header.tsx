import * as React from 'react';

export default () =>
  <header>
    {/*<div className="logo">
        {<a href="/">{{ $.Site.Title }}</a>}
    </div>
    </header>

    {{ if .IsHome }}
    <section class="fader">
    </section>

    <section class="intro">
        <div class="container">
        <h1>{{$.Site.Params.subtitle}}</h1>
        </div>
    </section>
    {{ else }}
    <section class="banner">
    </section>
    {{ end }}*/}
  </header>;